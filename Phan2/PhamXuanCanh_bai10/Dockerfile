# Sử dụng PHP 8.2 với Apache
FROM php:8.2-apache

# Thiết lập thư mục làm việc
WORKDIR /var/www/html

# Copy toàn bộ mã nguồn từ máy host vào container
COPY src/ /var/www/html/

# Mở cổng 80 để truy cập Apache
EXPOSE 80
